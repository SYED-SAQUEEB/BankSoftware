using System;

public partial class AboutUs : System.Web.UI.Page
{
    protected void Page_Load(object sender, EventArgs e)
    {
        // Any future server-side logic (currently just static content)
        // For example: loading team member data dynamically from DB
    }
}
